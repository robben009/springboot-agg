server:
  port: 8081

spring:
  application:
    name: dubbo-provider

dubbo:
  application:
    name: dubbo-provider
  scan:
    base-packages: com.robben
  protocol:
    port: -1
  registry:
    address: nacos://212.64.18.166:38080

#这个比较奇怪,如果改成了bootstrap.yml会存在启动问题
